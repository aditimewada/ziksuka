export class User{
    user_id: number;
    last_response: number;

    // constructor(data: any) {
    //     data = data || {};
    //     this.user_id = data.user_id;
    //     this.last_response = -1;
    // }
}